import React, { Component } from 'react';
import './songcreateform.scss';

export default class SongCreateFormComponent extends Component {

    render(){
        return (
            <form className="songform songform--size songform--theme">
                
            </form>
        )
    }
}